{
  "name": "rest-express",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build && cpx public/_redirects dist/public && esbuild server/index.ts --platform=node --bundle --format=esm --outdir=dist/server",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@babel/preset-typescript": "^7.23.0",
    "lightningcss": "^1.16.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.4.20",
    "esbuild": "^0.19.2",
    "cpx": "^1.5.0",
    "typescript": "^5.2.2"
  }
}
